import React from 'react';
import Logo from '../logo/logo'

const Header = (props) => {
    return (
        <header className="header">
              <Logo/>
        </header>
    );
};

export default Header;
